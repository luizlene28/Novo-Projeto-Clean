<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Severino Master Clean PRO</title>
    <style>
        :root {
            --primary: #6C3CE1;
            --primary-glow: #9B6DFF;
            --bg: #0f0a1a;
            --card-bg: #1a1030;
            --text-white: #ffffff;
            --accent1: #FF3D71;
            --accent2: #FFB347;
            --accent3: #00E5FF;
            --accent4: #76FF03;
            --accent5: #FF6EC7;
            --accent6: #FFD740;
            --surface-3d: #1e1338;
        }

        * {
            box-sizing: border-box;
        }   

        body {
            margin: 0;
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            background: radial-gradient(ellipse at 30% 20%, #1a1040 0%, #0a0618 40%, #05030f 100%);
            background-attachment: fixed;
            overflow: hidden;
            min-height: 100vh;
            position: relative;
        }

        /* Partículas de fundo sutis */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
            background:
                radial-gradient(circle at 15% 25%, rgba(108, 60, 225, 0.12) 0%, transparent 50%),
                radial-gradient(circle at 75% 60%, rgba(255, 61, 113, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 50% 80%, rgba(0, 229, 255, 0.06) 0%, transparent 50%),
                radial-gradient(circle at 85% 15%, rgba(255, 179, 71, 0.07) 0%, transparent 40%);
        }

        /* Orbes flutuantes decorativos */
        .bg-orb {
            position: fixed;
            border-radius: 50%;
            pointer-events: none;
            z-index: 0;
            filter: blur(80px);
            animation: floatOrb 12s ease-in-out infinite;
        }
        .bg-orb.orb1 {
            width: 300px;
            height: 300px;
            background: rgba(108, 60, 225, 0.25);
            top: -100px;
            left: -80px;
            animation-delay: 0s;
        }
        .bg-orb.orb2 {
            width: 250px;
            height: 250px;
            background: rgba(255, 61, 113, 0.2);
            bottom: -80px;
            right: -60px;
            animation-delay: -4s;
            animation-duration: 14s;
        }
        .bg-orb.orb3 {
            width: 200px;
            height: 200px;
            background: rgba(0, 229, 255, 0.18);
            top: 50%;
            left: 60%;
            animation-delay: -8s;
            animation-duration: 16s;
        }

        @keyframes floatOrb {
            0%,
            100% {
                transform: translate(0, 0) scale(1);
            }
            25% {
                transform: translate(40px, -30px) scale(1.15);
            }
            50% {
                transform: translate(-20px, 20px) scale(0.9);
            }
            75% {
                transform: translate(-35px, -15px) scale(1.1);
            }
        }

        /* HEADER COM LOGO 3D E TÍTULO DINÂMICO */
        .app-header {
            position: relative;
            z-index: 10;
            background: linear-gradient(135deg, #1e1045 0%, #281558 30%, #1a0f3c 60%, #12082e 100%);
            color: var(--text-white);
            padding: 14px 22px;
            display: flex;
            align-items: center;
            box-shadow:
                0 8px 32px rgba(0, 0, 0, 0.5),
                0 2px 8px rgba(108, 60, 225, 0.3),
                inset 0 1px 0 rgba(255, 255, 255, 0.05),
                0 1px 0 rgba(155, 109, 255, 0.2);
            border-bottom: 2px solid rgba(155, 109, 255, 0.35);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            transform-style: preserve-3d;
            perspective: 800px;
        }

        .app-header::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg,
                    transparent 0%, var(--accent3) 15%, var(--primary-glow) 30%,
                    var(--accent1) 50%, var(--accent2) 70%, var(--accent4) 85%, transparent 100%);
            opacity: 0.7;
            z-index: 1;
            animation: shimmerBar 4s ease-in-out infinite;
        }

        @keyframes shimmerBar {
            0%,
            100% {
                opacity: 0.5;
                background-position: 0% 50%;
            }
            50% {
                opacity: 0.9;
                background-position: 100% 50%;
            }
        }

        /* Container do cubo 3D */
        .cube-outer-wrapper {
            position: relative;
            z-index: 20;
            margin-right: 16px;
            flex-shrink: 0;
            filter: drop-shadow(0 8px 20px rgba(108, 60, 225, 0.5)) drop-shadow(0 0 30px rgba(155, 109, 255, 0.35));
            animation: cubeFloat 4s ease-in-out infinite;
        }

        @keyframes cubeFloat {
            0%,
            100% {
                transform: translateY(0);
            }
            30% {
                transform: translateY(-6px);
            }
            60% {
                transform: translateY(-2px);
            }
            85% {
                transform: translateY(-5px);
            }
        }

        .cube-scene {
            width: 54px;
            height: 54px;
            perspective: 500px;
            perspective-origin: 50% 50%;
        }

        .cube-3d {
            width: 54px;
            height: 54px;
            position: relative;
            transform-style: preserve-3d;
            animation: rotateCube 10s cubic-bezier(0.45, 0.05, 0.35, 0.95) infinite;
        }

        @keyframes rotateCube {
            0% {
                transform: rotateY(0deg) rotateX(15deg) rotateZ(0deg);
            }
            20% {
                transform: rotateY(120deg) rotateX(20deg) rotateZ(3deg);
            }
            40% {
                transform: rotateY(240deg) rotateX(10deg) rotateZ(-3deg);
            }
            55% {
                transform: rotateY(300deg) rotateX(22deg) rotateZ(5deg);
            }
            70% {
                transform: rotateY(380deg) rotateX(14deg) rotateZ(-2deg);
            }
            85% {
                transform: rotateY(460deg) rotateX(18deg) rotateZ(1deg);
            }
            100% {
                transform: rotateY(720deg) rotateX(15deg) rotateZ(0deg);
            }
        }

        .cube-face {
            position: absolute;
            width: 54px;
            height: 54px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 900;
            font-size: 26px;
            color: #fff;
            text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
            backface-visibility: visible;
            border: 2px solid rgba(255, 255, 255, 0.3);
            box-shadow: inset 0 2px 10px rgba(255, 255, 255, 0.2), inset 0 -2px 6px rgba(0, 0, 0, 0.3);
            letter-spacing: 1px;
            user-select: none;
            transition: box-shadow 0.5s;
        }

        .cube-face.front {
            background: linear-gradient(145deg, #FF3D71, #e0305e);
            transform: translateZ(27px);
        }
        .cube-face.back {
            background: linear-gradient(145deg, #00E5FF, #00b8d4);
            transform: rotateY(180deg) translateZ(27px);
        }
        .cube-face.right {
            background: linear-gradient(145deg, #FFB347, #ff9800);
            transform: rotateY(90deg) translateZ(27px);
        }
        .cube-face.left {
            background: linear-gradient(145deg, #9B6DFF, #7c4dff);
            transform: rotateY(-90deg) translateZ(27px);
        }
        .cube-face.top {
            background: linear-gradient(145deg, #76FF03, #64dd17);
            transform: rotateX(90deg) translateZ(27px);
        }
        .cube-face.bottom {
            background: linear-gradient(145deg, #FF6EC7, #f5009b);
            transform: rotateX(-90deg) translateZ(27px);
        }

        /* Brilho pulsante ao redor do cubo */
        .cube-outer-wrapper::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 70px;
            height: 70px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(155, 109, 255, 0.5) 0%, transparent 70%);
            z-index: -1;
            animation: pulseGlow 3s ease-in-out infinite;
            pointer-events: none;
        }

        @keyframes pulseGlow {
            0%,
            100% {
                transform: translate(-50%, -50%) scale(0.85);
                opacity: 0.6;
            }
            50% {
                transform: translate(-50%, -50%) scale(1.35);
                opacity: 1;
            }
        }

        .app-logo-img {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            margin-right: 14px;
            object-fit: cover;
            border: 2px solid rgba(255, 255, 255, 0.35);
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4);
            flex-shrink: 0;
            display: none;
            /* Mantido no HTML mas oculto; o cubo 3D é o destaque */
        }
        .header-text {
            display: flex;
            flex-direction: column;
        }
        .header-text b {
            font-size: 1.05rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-shadow: 0 2px 10px rgba(155, 109, 255, 0.6);
            background: linear-gradient(135deg, #fff 0%, #e0d5ff 50%, #fff 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 800;
        }
        .header-text span {
            font-size: 0.75rem;
            opacity: 0.85;
            font-weight: 400;
            letter-spacing: 1.5px;
            text-shadow: 0 1px 4px rgba(0, 0, 0, 0.5);
            color: #c9b8f0;
        }

        /* MENU DE ABAS ESTILO APP STORE 3D */
        .tabs {
            display: flex;
            background: linear-gradient(180deg, #1a1035 0%, #150c2a 100%);
            border-bottom: 1px solid rgba(155, 109, 255, 0.3);
            position: relative;
            z-index: 5;
            padding: 4px 12px;
            gap: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5), 0 1px 0 rgba(255, 255, 255, 0.03);
        }
        .tab {
            flex: 1;
            text-align: center;
            padding: 13px 10px;
            font-weight: 700;
            color: #8a7ab5;
            cursor: pointer;
            transition: all 0.35s cubic-bezier(0.25, 0.8, 0.25, 1.2);
            font-size: 0.8rem;
            letter-spacing: 1px;
            border-radius: 12px;
            position: relative;
            background: transparent;
            border: none;
            transform-style: preserve-3d;
            perspective: 400px;
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);
        }
        .tab:hover {
            color: #d5c8f5;
            background: rgba(155, 109, 255, 0.1);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(108, 60, 225, 0.3);
        }
        .tab:active {
            transform: translateY(1px) scale(0.96);
            transition: all 0.1s ease;
        }
        .tab.active {
            color: #fff;
            background: linear-gradient(145deg, #6C3CE1, #5a2ec9);
            box-shadow:
                0 8px 28px rgba(108, 60, 225, 0.5),
                0 2px 6px rgba(0, 0, 0, 0.4),
                inset 0 1px 0 rgba(255, 255, 255, 0.2),
                0 1px 0 rgba(155, 109, 255, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transform: translateY(-2px) scale(1.03);
            text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
            animation: activeTabPulse 2.5s ease-in-out infinite;
        }
        .tab.active::after {
            content: '';
            position: absolute;
            bottom: -6px;
            left: 50%;
            transform: translateX(-50%);
            width: 60%;
            height: 4px;
            background: var(--accent3);
            border-radius: 0 0 8px 8px;
            box-shadow: 0 4px 16px rgba(0, 229, 255, 0.6);
            animation: glowBar 2.5s ease-in-out infinite;
        }

        @keyframes activeTabPulse {
            0%,
            100% {
                box-shadow: 0 8px 28px rgba(108, 60, 225, 0.5), 0 2px 6px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.2), 0 1px 0 rgba(155, 109, 255, 0.5);
            }
            50% {
                box-shadow: 0 12px 36px rgba(108, 60, 225, 0.7), 0 4px 10px rgba(0, 0, 0, 0.5), inset 0 1px 0 rgba(255, 255, 255, 0.3), 0 2px 0 rgba(155, 109, 255, 0.7);
            }
        }

        @keyframes glowBar {
            0%,
            100% {
                opacity: 0.7;
                width: 60%;
            }
            50% {
                opacity: 1;
                width: 80%;
            }
        }

        /* Container do iframe com moldura 3D */
        .iframe-container {
            position: relative;
            z-index: 2;
            margin: 6px 10px 10px 10px;
            border-radius: 14px;
            overflow: hidden;
            box-shadow:
                0 12px 40px rgba(0, 0, 0, 0.6),
                0 2px 8px rgba(108, 60, 225, 0.25),
                inset 0 0 0 1px rgba(255, 255, 255, 0.05);
            border: 2px solid rgba(155, 109, 255, 0.3);
            background: #0d0820;
            height: calc(100vh - 140px);
        }
        iframe {
            width: 100%;
            height: 100%;
            border: none;
            border-radius: 11px;
            display: block;
        }

        /* Efeito de reflexo na moldura */
        .iframe-container::before {
            content: '';
            position: absolute;
            top: -1px;
            left: 20px;
            right: 20px;
            height: 1px;
            background: linear-gradient(90deg,
                    transparent, rgba(255, 255, 255, 0.4) 20%, rgba(255, 255, 255, 0.6) 50%, rgba(255, 255, 255, 0.4) 80%, transparent);
            z-index: 10;
            pointer-events: none;
            border-radius: 50%;
        }

        /* Scrollbar estilizada para o iframe (não se aplica diretamente, mas ao body) */
        ::-webkit-scrollbar {
            width: 6px;
        }
        ::-webkit-scrollbar-track {
            background: #0d0820;
            border-radius: 3px;
        }
        ::-webkit-scrollbar-thumb {
            background: #6C3CE1;
            border-radius: 3px;
        }
    </style>
</head>
<body>

    <!-- Orbes decorativas de fundo -->
    <div class="bg-orb orb1"></div>
    <div class="bg-orb orb2"></div>
    <div class="bg-orb orb3"></div>

    <!-- HEADER COM CUBO 3D -->
    <div class="app-header">
        <!-- Cubo 3D no topo esquerdo -->
        <div class="cube-outer-wrapper">
            <div class="cube-scene">
                <div class="cube-3d">
                    <div class="cube-face front">L</div>
                    <div class="cube-face back">C</div>
                    <div class="cube-face right">S</div>
                    <div class="cube-face left">M</div>
                    <div class="cube-face top">✦</div>
                    <div class="cube-face bottom">P</div>
                </div>
            </div>
        </div>

        <img src="imagens/logovelocimetro.jpg" alt="Logo" class="app-logo-img"
        onerror="this.style.display='none'">

        <div class="header-text">
            <b>Severino Master Clean</b>
            <span id="subtitulo-pagina">PAINEL DE CONTROLE</span>
        </div>
    </div>

    <!-- ABAS DE NAVEGAÇÃO -->
    <div class="tabs">
        <div class="tab active" onclick="mudar(this, 'paginas/painel.html', 'PAINEL DE CONTROLE')">PAINEL</div>
        <div class="tab" onclick="mudar(this, 'paginas/sensor.html', 'SENSORES DO HARDWARE')">SENSORES</div>
        <div class="tab" onclick="mudar(this, 'paginas/limpeza.html', 'OTIMIZAÇÃO MASTER')">LIMPEZA</div>
    </div>

    <!-- CONTEÚDO PRINCIPAL -->
    <div class="iframe-container">
        <iframe id="frame" src="paginas/painel.html"></iframe>
    </div>

    <script>
        function mudar(el, path, subtitulo) {
            document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
            el.classList.add('active');
            document.getElementById('frame').src = path;
            document.getElementById('subtitulo-pagina').innerText = subtitulo;
        }
    </script>
</body>
</html>